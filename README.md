# General Smart Medical Assistance System

A Streamlit prototype for a hospital-grade healthcare assistance platform.

## Run locally

```powershell
python -m pip install -r requirements.txt
python -m streamlit run streamlit_app.py
```

## Demo login

- Email: `patient@example.com`
- Mobile: `9876543210`
- Password: `demo123`
- CAPTCHA: enter the code shown on screen

This prototype uses local sample data and simulated disease predictions. It is not a medical diagnosis system.
